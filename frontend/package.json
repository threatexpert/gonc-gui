{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test:inline-qr": "tsc -p tsconfig.inline-qr-test.json && node -e \"const { spawnSync } = require('node:child_process'); const result = spawnSync(process.execPath, ['--test', require('node:path').resolve('node_modules/.cache/gonc-inline-qr-tests/tests/inlineQrState.test.js')], { stdio: 'inherit' }); process.exit(result.status ?? 1);\"",
    "test:send-content": "tsc -p tsconfig.send-content-test.json && node -e \"const { spawnSync } = require('node:child_process'); const result = spawnSync(process.execPath, ['--test', require('node:path').resolve('node_modules/.cache/gonc-send-content-tests/tests/sendContentState.test.js')], { stdio: 'inherit' }); process.exit(result.status ?? 1);\"",
    "test": "npm run test:inline-qr && npm run test:send-content"
  },
  "dependencies": {
    "qrcode": "^1.5.4",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "zxing-wasm": "^3.1.0"
  },
  "devDependencies": {
    "@types/qrcode": "^1.5.6",
    "@types/react": "^18.0.17",
    "@types/react-dom": "^18.0.6",
    "@vitejs/plugin-react": "^2.0.1",
    "typescript": "^4.6.4",
    "vite": "^3.0.7"
  }
}
